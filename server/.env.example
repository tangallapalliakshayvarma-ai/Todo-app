# Copy this file to .env and fill your MongoDB URI if needed
# For local MongoDB (default), you can skip setting MONGO_URI.
# For MongoDB Atlas, paste the full connection string:
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/<dbname>?retryWrites=true&w=majority&appName=<appname>

PORT=5000
# MONGO_URI=
